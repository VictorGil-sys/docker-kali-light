version: '3.4'

services:
  kali-light:
    image: "kali-light"
    volumes:
      - /mnt/share-kali-light:/share
      - /mnt/share-kali-light/.bash_history:/root/.bash_history
    network_mode: host
